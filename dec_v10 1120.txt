增加数据发送中的同步操作：同DAB_V10_20140126配合使用；
	1、增加一条用户指令:同步应答指令，指令码为0x25；
	2、DEC根据用户数据指令的长度值：若为7998，，向DAB发送一个指令码为0x25的同步应答指令，数据长度为0；
	3、TX_FIFO的大小为8kDW；即发送250个TLP包后产生一次中断。第一个TLP包带指令信息，即事件数为32k-2个——7998个；	
	双线程处理：一个线程处理文件，一个线程接收数据；

0716:
	基于v5，与da_v11_20140716配合使用。经过验证。实现DAS的功能。
	
731:
	基于V6，dec_v10板。
806:
	在速率检测模块中，低速档增加了190个事件/包的档位。
915:
	机电系统的信号需经过光纤上传，故用户的协议包的打包工作应由DA完成。DEC检测FIFO数据，按包转发到pcie总线上。支持功能有：
	DMA读，由ACS准备好数据并使能DMA读，DEC发起读请求、接收总线的CPLD包，并将数据写入发送到Aurora的FIFO中。
	DMA写，由ACS准备好并使能DMA写，DEC发起写请求、发送数据包。当DEC从Aurora接收到数据，但ACS并未使能DMA写时，由DEC通过14号寄存器告知ACS需要进行DMA写传输，ACS发起一次DMA写。
	寄存器操作，即内存操作。存放DMA操作的相关信息及系统的其他信息。
1111:
	增加用户包编号检查。
1118:
	优化同步机制。停止后，同步计数器清零。
1120：
	优化定时采集。定时采集改为由acs定时，定时到发停止命令。DEC内部将没有采集定时功能。